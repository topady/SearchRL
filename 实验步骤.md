# SearchRL 实验步骤

## 第一节：项目修改说明

本项目原始版本对 `flash-attn` 和 `vllm` 有硬依赖。这两个包存在以下问题：

| 依赖包 | 问题 |
|--------|------|
| `flash-attn` | 不支持 Windows；Blackwell (RTX 50 系列) 需要 >= 2.8.0 + CUDA 12.8+，编译极易 OOM |
| `vllm` | Linux-only；v0.6.3 绑定 `torch==2.4.0`，Blackwell (RTX 50 系列) 需要 `torch>=2.7.0+cu128` |

我们对代码做了以下修改，使这两个依赖变为**完全可选**：

### 修改 1：注意力后端可配置化

- `verl/trainer/config/ppo_trainer.yaml` 和 `sft_trainer.yaml` 新增 `attn_implementation` 配置项，默认 `sdpa`（PyTorch 内建注意力，零额外依赖）
- `verl/workers/fsdp_workers.py` 和 `verl/trainer/fsdp_sft_trainer.py` 中原硬编码的 `attn_implementation='flash_attention_2'` 改为从配置读取
- FlashAttention2 的 monkey patch 只在 `attn_implementation='flash_attention_2'` 时生效

### 修改 2：flash-attn 导入解耦

- 新建 `verl/utils/bert_padding.py` — 纯 PyTorch 实现的 `pad_input`、`unpad_input`、`index_first_axis`
- 所有 `from flash_attn.bert_padding import ...` 替换为 `from verl.utils.bert_padding import ...`
- `parallel_attention.py` 中的 `apply_rotary_emb` 加 try/except，缺失时自动回退到已有的纯 PyTorch 实现
- `requirements.txt` 中 `flash-attn` 改为可选（注释掉），新增 `einops`

### 修改 3：推理后端切换

- `ppo_trainer.yaml` 中 `rollout.name` 默认值从 `vllm` 改为 `hf`
- 所有训练脚本中移除 vllm 专属参数（`tensor_model_parallel_size`、`gpu_memory_utilization`、`VLLM_ATTENTION_BACKEND`）
- 训练脚本中 `rollout.name=vllm` 改为 `rollout.name=hf`
- `HFRollout` 直接调用 `model.generate()`，使用配置中的 `attn_implementation`（默认 `sdpa`），不需要任何额外推理引擎

### 修改 4：自定义奖励函数集成

- `verl/utils/import_utils.py` 新增 `load_extern_function(path, name)` 动态函数加载器
- `main_ppo.py` 和 `main_ppo_format.py` 的 `RewardManager` 支持 `custom_reward_function` 配置
- 当 YAML 配置了 `custom_reward_function` 时，RewardManager 自动加载并使用该函数；未配置时回退到原有 `qa_em` 奖励
- 整个 `reward_model` 数据字典作为 `extra_info` 传入奖励函数
- `ppo_trainer.yaml` 默认配置使用 `reward_functions/rubric_merged.py:compute_score`（5 维度 Rubric 奖励）

### 依赖状态总览

| 包 | 修改前 | 修改后 |
|----|--------|--------|
| `flash-attn` | 硬依赖，编译失败则无法安装 | **可选**，不安装时自动使用纯 PyTorch 回退 |
| `vllm` | 硬依赖，绑死 torch 2.4.0 | **可选**，默认使用 HF generate 推理 |
| `einops` | 未声明（flash-attn 间接提供） | 显式依赖 |

---

## 第二节：环境配置

### 1. 系统要求

| 组件 | 要求 |
|------|------|
| 操作系统 | Linux（原生或 WSL2） |
| GPU | NVIDIA GPU（Blackwell SM120 需 PyTorch >= 2.7.0） |
| Python | 3.10+ |

### 2. 创建环境并安装依赖

```bash
# 创建 conda 环境
conda create -n searchrl python=3.10 -y
conda activate searchrl

# 安装 PyTorch（Blackwell GPU 需要 >= 2.7.0+cu128）
# 非 Blackwell GPU 可使用默认版本
pip install torch==2.11.0 torchvision --index-url https://download.pytorch.org/whl/cu128

# 升级 xformers（兼容新版 PyTorch）
pip install xformers==0.0.35

# 安装其余依赖
pip install -r requirements.txt --no-build-isolation

# 编译安装 verl
pip install -v .
```

---

## 第三节：配置文件说明

核心配置文件为 `verl/trainer/config/ppo_trainer.yaml`。训练脚本通过 Hydra CLI 参数覆盖默认值。

### 关键配置项

```yaml
actor_rollout_ref:
  model:
    use_remove_padding: False       # rmpad 优化（纯 PyTorch 回退，无需 flash-attn）
    attn_implementation: sdpa       # sdpa / flash_attention_2 / eager
  rollout:
    name: hf                        # hf (HuggingFace) 或 vllm
    n_agent: 1                      # GRPO 时设为 5

# 自定义奖励函数（5 维度 Rubric）
custom_reward_function:
  path: reward_functions/rubric_merged.py
  name: compute_score
```

### 奖励函数说明

默认使用 5 维度 Rubric 奖励函数（`reward_functions/rubric_merged.py`），替代原有的二元 Exact Match 奖励。

| 维度 | 权重 | 说明 |
|------|------|------|
| D1: Query 质量 | 0.15 | 搜索 query 的相关性、长度合理性、非复制性 |
| D2: 重复搜索惩罚 | 0.15 | 惩罚多轮次中高度相似的 query |
| D3: 检索结果利用度 | 0.20 | 检索文档是否被推理过程引用 |
| D4: 推理逻辑一致性 | 0.15 | 答案是否有 thinking 支撑 |
| D5: 答案 F1 | 0.35 | SQuAD 标准 token-level F1（连续信号） |

**EM Gate 机制**：当答案 F1=0 时，过程分（D1-D4）衰减至 30%，保留梯度信号避免训练早期全零 reward。

**消融实验**：修改 `custom_reward_function.path` 即可切换不同奖励函数：

```yaml
# 只看答案 F1
custom_reward_function:
  path: reward_functions/dim5_answer_f1.py
  name: compute_score

# 回退到原有 EM 奖励（注释掉 custom_reward_function 即可）
# custom_reward_function:
#   path: reward_functions/rubric_merged.py
#   name: compute_score
```

**自定义权重**：通过数据集 `reward_model` 字段传入 `extra_info`，例如：

```python
# 在数据预处理阶段向每条样本添加
reward_model = {
    "ground_truth": {"target": "1889"},
    "weights": {"query_quality": 0.10, "answer_f1": 0.50, ...},
    "em_decay": 0.3,
}
```

### 注意力后端选择

| attn_implementation | 需要 flash-attn | 适用场景 |
|---------------------|----------------|----------|
| `sdpa`（默认）      | 否              | **通用训练**，兼容性最好 |
| `flash_attention_2` | 是              | 高性能场景，需成功安装 flash-attn |
| `eager`             | 否              | 调试用，最慢 |

### 推理后端选择

| rollout.name | 需要 vllm | 推理引擎 | 适用场景 |
|-------------|-----------|----------|----------|
| `hf`（默认）  | 否         | HF model.generate() | **单卡训练**，3B-7B 模型 |
| `vllm`       | 是         | vLLM 引擎 | 大模型 TP 推理，多卡连续批处理 |

### 训练脚本关键参数

运行 `train_grpo.sh` 时可通过 Hydra CLI 覆盖：

```bash
# 注意力后端
actor_rollout_ref.model.attn_implementation=sdpa

# 推理后端
actor_rollout_ref.rollout.name=hf

# rmpad 优化
actor_rollout_ref.model.use_remove_padding=True

# GRPO 每个 prompt 采样数
actor_rollout_ref.rollout.n_agent=5
```

---

## 第四节：数据格式要求

Rubric 奖励函数要求模型生成遵循 Search-R1 标签格式：

```
<think>推理过程1</think>
<search>搜索 query 1</search>
<information>
Document [1](Title: 标题1) 正文1
Document [2](Title: 标题2) 正文2
</information>
<think>推理过程2（基于检索结果）</think>
<search>搜索 query 2</search>
<information>
Document [1](Title: 标题3) 正文3
</information>
<think>推理过程3</think>
<answer>最终答案</answer>
```

| 标签 | 用途 | 影响维度 |
|------|------|----------|
| `<think>` | 推理过程 | D3, D4 |
| `<search>` | 搜索 query | D1, D2 |
| `<information>` | 检索返回文档 | D3 |
| `<answer>` | 最终答案 | D4, D5 |

`ground_truth` 格式：

```python
{
    "target": "1889",      # 标准答案（必填）
    "question": "...",     # 原始问题（可选，影响 D1 质量）
}
```

---

## 第五节：运行实验

### 1. 准备数据

按项目原始文档准备 NQ/Search 数据集。

### 2. 启动检索服务

```bash
bash retrieval_launch.sh
```

### 3. 启动训练

```bash
# GRPO 训练（Qwen2.5-3B）
bash train_grpo.sh

# 或 PPO 训练
bash train_ppo.sh
```

训练脚本会通过 Hydra 覆盖 YAML 默认值，可编辑脚本中的 `BASE_MODEL` 和 `EXPERIMENT_NAME` 来切换模型和实验名。

### 4. 多卡训练

多卡场景如需高性能推理，可以安装好 vllm 后将 `rollout.name` 改回 `vllm` 以使用连续批处理加速。单卡 3B 模型场景下 HFRollout 性能差距可忽略。

---

## 第六节：常见问题

| 问题 | 解决方案 |
|------|----------|
| `no kernel image available for sm_120` | PyTorch 版本太旧，升级到 `>= 2.7.0+cu128` |
| vllm 提示 `torch==2.4.0` 不兼容 | 使用 HF rollout（默认），不需要 vllm |
| flash-attn 编译失败 | 使用 `sdpa` 注意力（默认），不需要 flash-attn |
| `ModuleNotFoundError: No module named 'flash_attn'` | 正常现象，代码已解耦，无 flash-attn 时自动回退 |
| `import vllm` 失败 | 正常现象，HF rollout 不需要 vllm（如需使用 vllm 后端，请安装兼容的 vllm 版本） |
| HFRollout 生成速度慢 | 3B 模型单卡场景下差距不大；如需加速可安装 flash-attn + vllm 并切换后端 |
| 奖励始终为 0 | 检查模型输出是否包含 `<answer>...</answer>` 标签；无标签时奖励为 0 |
| 想回到原始 EM 奖励 | 在配置中删除或注释掉 `custom_reward_function` 块即可 |
| 某个维度总是低分 | 检查对应标签是否正确生成（如无 `<search>` 则 D1=0, D2=1） |
